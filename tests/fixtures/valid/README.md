# Valid VASP input fixtures

Canonical **valid** fixtures referenced by `lsp-capabilities.json` → `fixturePaths.valid`.

| Fixture | Role |
|---------|------|
| `minimal.INCAR` | Clean INCAR with only upstream-valid tags |

Rule-level valid fixtures remain under `tests/fixtures/rules/*/valid_*`.
